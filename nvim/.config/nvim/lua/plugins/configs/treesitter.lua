local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "python", "c", "foam", "cpp", "fortran"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
